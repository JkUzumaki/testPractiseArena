//To set a bit @ specific position
//	a = a | (1 << position)

//To unset a bit @ specific position
//	a = a & (~(1 << position))

//To check is a bit @ any position is 1 or 0
// i = a & (1 << position)
// if(i > 0)
//	bit is 1 @ position
// else
//	bit is 0 @ position
